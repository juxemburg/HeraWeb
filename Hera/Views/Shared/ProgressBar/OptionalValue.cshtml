@model PropertyViewModel

@if (Model.Visible)
{
    var val = Math.Round(Model.Value, 2);
    <dt>@Model.Name (@val %)</dt>
    <dd>
        @Html.Partial("ProgressBar/Success", Model.Value)
    </dd>
}
